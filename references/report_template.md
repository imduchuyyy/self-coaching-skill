# Report template

Keep the whole thing under ~1 page of terminal output. Write it in the user's
language. Replace every bracketed part with what you actually observed.

---

**AI usage review — [N] sessions, [date range]**
[X] quick tasks / [Y] deep tasks. Groups 1–4 scored on the deep tasks.

| Metric family | Score | What the history shows |
|---|---|---|
| 1. Input Quality | [n]/5 | [count-based observation, e.g. "12 of 21 deep tasks opened with under 15 words; 3 stated a goal"] |
| 2. Interaction Depth | [n]/5 | [e.g. "median 3 human turns; 4 sessions ended after the first answer; 5 real challenges"] |
| 3. Verification | [n]/5 | [e.g. "an error was pasted back in 6 sessions; the AI was corrected in 3; no source ever requested"] |
| 4. Skill Transfer | [n]/5 | [e.g. "7 'why' questions across 21 sessions; blog-writing prompts unchanged over 5 sessions"] |
| 5. Context Fit | [n]/5 | [e.g. "quick lookups handled correctly; 4 production changes opened like quick tasks"] |

**Strengths**

1. [Strength] — *"[real quote]"* ([date]). [Why it worked.]
2. [Strength] — *"[real quote]"* ([date]).

**Gaps to close**

1. **[Biggest gap]** — [what happens and how often]. e.g. *"[real weak quote]"*
   ([date]) → [the cost: N repair turns, wrong output, rework].
2. **[Second gap]** — [same shape].
3. **[Third gap]** — [same shape].

**Rewrite of one real prompt**

> Before: *"[the user's actual weak prompt, verbatim]"*

> After: *"[rewritten: goal + constraints + context + output format, in the
> user's own voice and language — not a generic template]"*

[One line on which part of the rewrite does the work.]

**Three things to try this week**

1. [Concrete habit] → track: [the signal that should move, e.g. "repair
   turns on deep tasks drop below 2"].
2. [Concrete habit] → track: [signal].
3. [Concrete habit] → track: [signal].

---

Then offer to re-run the review in a few weeks to compare.
